const DB_NAME = "product-manager"
const BASE_URL = "/api/v1"
const productAvailabilityEnum = Object.freeze({
	stockIn: "Stock In",
	stockOut: "Stock Out",
})

export { DB_NAME, BASE_URL, productAvailabilityEnum }
